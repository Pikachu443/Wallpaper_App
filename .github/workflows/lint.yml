name: Lint
on: push
jobs:
  lint:
    name: Lint
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download sigcheck.exe
        run: |
          Invoke-WebRequest -Uri 'https://live.sysinternals.com/sigcheck.exe' -OutFile 'sigcheck.exe'

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: pip install pefile

      - name: Update EXE Metadata
        run: python update.py ./percy.exe


      # - name: Download Resource Hacker
      #   run: |
      #     Invoke-WebRequest -Uri 'http://www.angusj.com/resourcehacker/resource_hacker.zip' -OutFile 'resource_hacker.zip'
      #     Expand-Archive -Path resource_hacker.zip -DestinationPath ./resource_hacker

      # - name: Update EXE Metadata and Icon
      #   run: |
      #     ./resource_hacker/ResourceHacker.exe -open percy.exe -save new.exe -action addoverwrite -res versioninfo.rc

      # - name: Download rcedit
      #   run: |
      #     Invoke-WebRequest -Uri https://github.com/electron/rcedit/releases/download/v1.1.1/rcedit-x64.exe -OutFile rcedit.exe
      # - name: Edit metadata
      #   run: |
      #     ./rcedit.exe "./percy.exe" --set-version-string CompanyName "BrowserStack Inc." --set-version-string FileDescription "Percy CLI Binary" --set-version-string InternalName "Percy CLI" --set-version-string LegalCopyright "Copyright BrowserStack Limited" --set-version-string OriginalFilename "Percy CLI" --set-version-string ProductName "Percy CLI" --set-version-string ProductVersion "1.28.8" --set-version-string BinaryVersion "1.28.8" --set-file-version "1.28.8" --set-icon "./app/percy.ico"
      # - name: Debug file system
      #   run: |
      #     dir  # List directory contents for debugging
      - name: Verify executable
        run: ./percy.exe --version
      # - name: Upload win artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: percy.exe
      #     path: percy.exe

      - name: Check executable with sigcheck
        shell: cmd
        run: |
          .\sigcheck.exe -accepteula -q -a -i "percy.exe"
